# Copy to .env and fill in for your environment (local/.env is gitignored).
# Local values below match the default local Supabase stack (see docs/ENVIRONMENT.md).

DATABASE_URL=postgresql://postgres:postgres@127.0.0.1:54322/postgres
JWKS_URL=http://127.0.0.1:54321/auth/v1/.well-known/jwks.json
PORT=8080
